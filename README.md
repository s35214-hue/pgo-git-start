This repository contains code which displays "Hello World!", as well as:
  - A method which removes any 'x' characters from a sentence;
  - A method which calculates the fibonacci value at a given point;
  - A method which returns the factorial of a number;
  - A method which reverses a string;
And a method which locates the most frequently repeated word in an Array List of strings.


